{
  "name": "sass-intro",
  "version": "0.0.1",
  "description": "Just some experiments about SASS",
  "main": "index.js",
  "scripts": {
    "start": "./node_modules/.bin/node-sass --watch --output dist --source-map true --source-map-contents sass --output-style nested sass/styles.scss",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "SASS",
    "experiment",
    "introduction"
  ],
  "author": "Valentin Vago <zeropaper@irata.ch>",
  "license": "MIT",
  "devDependencies": {
    "node-sass": "^4.5.3"
  }
}
